{
  "name": "analyzer",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "analyze": "docker compose run --rm analyzer bun run src/index.ts",
    "db:push": "docker compose run --rm analyzer bunx prisma db push",
    "prisma:generate": "docker compose run --rm analyzer bunx prisma generate"
  },
  "dependencies": {
    "@octokit/rest": "^21.1.1",
    "@prisma/client": "^6.0.0",
    "@types/bad-words": "^3.0.3",
    "bad-words": "^3.0.4",
    "ioredis": "^5.6.0",
    "simple-git": "^3.27.0"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prisma": "^6.0.0",
    "typescript": "^5.8.2"
  },
  "trustedDependencies": [
    "@prisma/client"
  ]
}
