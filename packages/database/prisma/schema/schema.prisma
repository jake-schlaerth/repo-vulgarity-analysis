generator client {
    provider        = "prisma-client-js"
    previewFeatures = ["prismaSchemaFolder"]
    // output          = "../dist/client"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}
