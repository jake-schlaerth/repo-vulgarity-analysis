{
  "name": "repo-vulgarity-analysis",
  "private": true,
  "workspaces": [
    "analyzer",
    "frontend",
    "backend"
  ],
  "scripts": {
    "analyzer": "bun run --cwd analyzer",
    "frontend": "bun run --cwd frontend",
    "backend": "bun run --cwd backend"
  }
}
